# ===== Book Notes — Production env =====

# Strong random secret for cookies/sessions
SESSION_SECRET=change-me

# Base64 of your Google Service Account JSON (production project)
FIREBASE_SA_JSON=<paste-base64-here>

# Google OAuth (production credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
CALL_BACK_URL=https://your-service.onrender.com/auth/google/books
# Optional: restrict to a specific Google Workspace domain
ALLOWED_GOOGLE_DOMAIN=

# App mode
NODE_ENV=production
USE_REACT=true

# Do NOT set FIRESTORE_EMULATOR_HOST in production.

# RAG / Search backends
# Postgres with pgvector (for Neon use ?sslmode=require; for Render self-hosted use ?sslmode=disable)
DATABASE_URL=
# Redis cache (Upstash recommended: rediss://...)
REDIS_URL=

# OpenAI (embeddings + optional rerank)
OPENAI_API_KEY=
EMBED_MODEL=text-embedding-3-small
EMBED_DIM=1536
# Optional reranking via LLM
USE_RERANK=false
RERANK_TIMEOUT_MS=6000

# Metrics (protect /metrics endpoint)
METRICS_TOKEN=
